# artiz-cod-form-worker

This project contains the initial foundation for a Shopify app-style Cloudflare Worker.

## Structure

- worker.js: main entry point
- routes/: route handlers
- graphql/: GraphQL query helpers
- utils/: helper utilities
